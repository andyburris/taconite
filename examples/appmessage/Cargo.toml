[package]
name = "appmessage"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
pebble-rust = { path = "../../../pebble-rust" }
taconite = { path = "../.." }

[profile.release]
opt-level = "s"
